import 'package:flutter/material.dart';

class VerticalSpace16Widget extends StatelessWidget {
  const VerticalSpace16Widget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16,
    );
  }
}
